# 1. בסיס האימג' - גרסה קלה של פייתון
FROM python:3.9-slim

# 2. הגדרת תיקיית העבודה בתוך הקונטיינר
WORKDIR /app

# 3. התקנת Flask (הספריה היחידה שאנחנו צריכים)
RUN pip install flask

# 4. העתקת הקוד שלנו מהמחשב לתוך הקונטיינר
COPY app.py .

# 5. הגדרת הפורט שהקונטיינר יחשוף (תיעודי בעיקר)
EXPOSE 8080

# 6. הפקודה שתריץ את האפליקציה כשהקונטיינר עולה
CMD ["python", "app.py"]